This configuration can/should be copy to the nginx web server configuration. This will serve as proxy(reverse) to the firecrest home page.

# Setting
- Redirect HTTP to HTTPS
- Certicates (self-signed)
- Proxy location